[[plugins]]
package = "@netlify/plugin-sitemap"

  [plugins.inputs]
  buildDir = "public"
  # disable pretty URLS and keep `index.html` & trailing `.html` file references in paths
   prettyURLs = true
   trailingSlash = true
  baseUrl = "https://www.kitchenkatanas.co.uk"

